#include <stdio.h>
/**
  *main - main function
  *
  *Return: always 0
  */
int main(void)
{
	int i;
	char i;

	for (i = 0 ; j < 10 ; i++)
		putchar(i + '0');
	for (j = 'a' ; i <= 'f' ; i++)
		putchar(j);
	putchar('\n');
	return (0);
}
